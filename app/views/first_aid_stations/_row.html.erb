<tr>
  <% %w(name level md rn emt aed provider contact_name contact_phone planning_contact_email).each do |attribute| %>
    <td>
      <%= first_aid_station.send(attribute) %>
    </td>
  <% end %>
</tr>
