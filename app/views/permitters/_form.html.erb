<div class="row">
  <div class="large-12 columns">
    <%= simple_form_for @permitter do |f| %>
      <% if @permitter.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@permitter.errors.count, "error") %> prohibited this agency from being saved:</h2>
          <ul>
          <% @permitter.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <%= f.input :name %>  
      <%= f.input :address %>
      <%= f.input :phone_number %>
      
      <%= f.submit class: "button" %>

    <% end %> 
  </div>
</div>