$("#<%= @comment.element_id %>").parents(".comment-section").children(".existing-comments").html('<%= escape_javascript(render partial: "existing_comments", locals: { plan: @plan, element_id: @comment.element_id }) %>')
