var section = $("#<%= @comment.element_id %>").closest(".comment-section");
section.replaceWith('<%= j render "plans/comment_form", plan: @plan, title: @comment.element_id.gsub("_comment_text", "") %>');
$(".plan-action").replaceWith('<%= j render "plans/actions", f: form_builder_for(@plan), plan: @plan %>');
