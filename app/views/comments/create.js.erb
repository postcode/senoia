var section = $("#<%= @comment.element_id %>").closest(".comment-section");
section.find(".existing-comments").html('<%= j render partial: "plans/existing_comments", locals: { plan: @plan, element_id: @comment.element_id } %>');
section.find(".new-comment-area").hide();
section.find("textarea").val("");
